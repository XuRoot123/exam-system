spring:
  application:
    name: exam-system
  datasource:
    password: muzichuan26
    username: root
    url: jdbc:mysql://localhost:3306/exam_system?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms
server:
  port: 7000
logging:
  level:
    web: trace
mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: cn.com.xuroot.entity
  configuration:
    map-underscore-to-camel-case: true
